package com.easygaadi.trucksmobileapp;

public class ChecklistMergedAdapter extends MergeAdapter {
    @Override
    public boolean isEnabled(int position) {
        return super.isEnabled(position);
    }
}
